# Native app + App Store plan — SafePass Manager

> **Status:** decisions captured 2026-07-22; **not yet implemented.** Targets: public
> Apple App Store + Google Play. This doc exists so we don't re-derive this path.

## The problem we're designing against (Apple Guideline 4.2)
Every SafePass app loads a hosted URL in a WebView (Capacitor `server.url`). On the
**public** App Store that's the exact shape Apple scrutinizes under **Guideline 4.2**
("this is just a website you could visit in Safari — minimum functionality"). The
**kiosk app already fought and won a 4.2 review** — reusable precedent lives in
`safepass-kiosk-web/docs/app-store-submission/dispute-prep.md`. It won on: native
depth + native OAuth + a browser-access gate + B2B framing.

Manager is thinner than kiosk, so it needs a deliberate native-depth story.

## Decisions

### Architecture — stay live-web-view (do NOT bundle)
Keep `server.url` (hosted origin) so web updates ship without an App Store resubmit.
Manager will keep evolving (native-depth roadmap below), so instant updates matter.
Its 4.2 defense comes from **native depth**, not bundling. (Contrast: the mapping app
IS bundling — different tradeoff, see its plan; it changes least so it tolerates slow
updates, and it needs bundling because it has almost no native depth.)

### Auth — port kiosk's native OAuth (Browser + custom scheme)  ← #1, fixes a LIVE bug
**Current native sign-in is BROKEN.** Manager uses the in-place
`window.location.assign` model but lacks `server.allowNavigation`, so the WebView
punts the Cognito redirect to the **external browser**; login completes there and
strands (lands on the app page in the browser, never returns to the app).

Fix = adopt kiosk's proven pattern (NOT just adding `allowNavigation`):
- `@capacitor/browser` (`Browser.open`, SFSafariViewController / Custom Tab) for the
  Cognito Hosted UI on native; `window.location.assign` on web. *(dep already present)*
- Custom scheme `safepassmanager://` redirect on native — **`.env` already defines**
  `VITE_COGNITO_NATIVE_REDIRECT_URI=safepassmanager://localhost/auth/callback` and the
  logout equivalent, so this was clearly the original intent before the 2026-07-13
  "no custom scheme" switch.
- `@capacitor/app` `appUrlOpen` listener catches the callback, closes the browser,
  exchanges the PKCE code. *(dep already present)*
- Register `safepassmanager://` in the native projects (iOS `Info.plist`
  `CFBundleURLTypes`; Android manifest intent-filter). These were freshly generated by
  `cap add` and do NOT have the scheme yet.

**Why kiosk's pattern over in-WebView (`allowNavigation`):** it's Apple's blessed OAuth
surface AND a native-depth signal for 4.2. In-WebView OAuth would *probably* work
technically on iOS (Capacitor supports `allowNavigation` on WKWebView), but the driver
here is App Store review, not function — kiosk chose Browser+scheme specifically for its
4.2 defense (no documented iOS technical failure of in-WebView was found). This reverses
manager's earlier "no custom scheme" decision, which predates the public-store 4.2
requirement.

> **BACKEND DEPENDENCY (owner, blocks this):** the Cognito app client must allow
> `safepassmanager://localhost/auth/callback` (and `…/auth/logout`) as callback/logout
> URLs. Kiosk's client already has its `safepasskiosk://` equivalents — mirror that.

### Native-depth roadmap (compounds the 4.2 defense over time)
- **Now:** native OAuth (above).
- **Soon:** Camera for visitor photos (`@capacitor/camera` + `NSCameraUsageDescription`
  already in place).
- **Later:** Push notifications.
These are real features *and* 4.2 evidence — roughly kiosk's winning recipe.

### Device target — universal (iPhone + iPad) is fine
Manager is a front-desk tool; iPad is plausible. Revisit only if an iPad-specific
issue surfaces. (Mapping goes iPhone-first — different rationale, see its plan.)

### Reviewer access
Login-gated; MFA is pool-enforced (currently temporarily disabled — provide the demo
user's TOTP seed via the store console when re-enabled, never in the repo). Real
check-in/out isn't completable in a demo org → reviewer notes + optional demo video.
See `store/app-review-access.md`.

## Open items (to prioritize)
- [ ] **Cognito client:** register `safepassmanager://…` callback + logout URLs *(backend, blocks OAuth)*
- [ ] Port kiosk native OAuth into `src/pages/Login.jsx` + `src/lib/cognitoHostedUi.js`
- [ ] Register `safepassmanager://` scheme (iOS Info.plist + Android manifest)
- [ ] Update CLAUDE.md / build-plan: the "no custom scheme" invariant is reversed
- [ ] Rebuild + test sign-in end-to-end on a real iOS device and Android
- [ ] Camera (visitor photos) — when the feature lands
- [ ] Push notifications — later
- [ ] Decide whether to add a browser-access gate (kiosk's `VITE_BLOCK_BROWSER_ACCESS`)
